# PaperLoom 自动更新发布指南

PaperLoom 的 Windows 自动更新使用 `NSIS + electron-updater + GitHub Releases`。当前发布仓库为 `https://github.com/GreyWolf1101/PaperLoom-Releases`，应用源码不需要上传到该仓库。

## 首次启用

1. 当前使用的公开 HTTPS 更新目录为：

   `https://github.com/GreyWolf1101/PaperLoom-Releases/releases/latest/download/`

2. `electron/update-config.json` 已经写入该地址。只有更换 GitHub 用户名或仓库时才需要修改。
3. 将 `package.json` 中的版本号递增，例如从 `0.9.2` 改为 `0.9.3`。
4. 执行：

   `npm run dist:update`

5. 在 GitHub 仓库中创建与版本号一致的 Release（例如标签 `v0.9.2`），并上传 `release` 目录中生成的以下文件：

   - `PaperLoom-Setup-版本号.exe`
   - `PaperLoom-Setup-版本号.exe.blockmap`
   - `latest.yml`

6. 将 Release 标记为正式版并发布，不要选择 Pre-release。用户第一次仍需手动安装这个“支持自动更新的版本”；从下一版开始，应用可以自动检查、下载并提示重启安装。

## 后续发布

1. 修改版本号，并在 `release-notes.md` 中写好本次更新说明；这些内容会自动写入 `latest.yml` 并显示在软件的更新页面中。
2. 执行 `npm run dist:update`。
3. 在 `PaperLoom-Releases` 创建新版本 Release，将新的安装包、blockmap 和 `latest.yml` 一起上传。
4. 保留至少一个上一版本安装包，确认新版本可以正常回滚后再清理。

## 安全要求

- 正式发布建议始终使用同一张 Windows 代码签名证书。
- 更新目录只能通过 HTTPS 访问。
- 不要手工修改 `latest.yml` 中的 SHA-512、文件名或大小。
- 创建 Release 时先保存为草稿，上传安装包和 blockmap 后再上传 `latest.yml`，确认三个文件齐全后统一发布。
- 便携版收到更新后会迁移到安装版；用户文献元数据与设置保存在 Windows 用户数据目录，不会因安装更新被清除。
